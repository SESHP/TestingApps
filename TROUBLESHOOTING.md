# 🔧 Решение проблем с реферальной системой

## Проблема: Реферальная ссылка не работает

### Диагностика

1. **Откройте консоль браузера (F12)**
   
   Должны быть логи:
   ```
   📌 Реферальный код из Telegram start_param: ABC12345
   или
   📌 Реферальный код из URL параметра: ABC12345
   ```

2. **Проверьте логи backend**
   
   ```bash
   cd backend
   npm run dev
   ```
   
   Должны появиться:
   ```
   📥 Запрос инициализации: { hasInitData: true, referralCode: 'ABC12345' }
   🔍 Поиск реферера с кодом: ABC12345
   ✅ Найден реферер: { id: 123456, name: 'Test User' }
   ✅ Пользователь создан: { telegram_id: ..., referred_by: 123456 }
   ✅ Реферальная связь создана: { referrerId: 123456, referredId: 789012 }
   ```

3. **Проверьте базу данных напрямую**
   
   ```bash
   psql -d ton_guarantee
   
   -- Проверяем пользователей
   SELECT telegram_id, first_name, referral_code, referred_by 
   FROM users 
   ORDER BY created_at DESC;
   
   -- Проверяем реферальные связи
   SELECT * FROM referrals ORDER BY created_at DESC;
   ```

### Частые причины

#### 1. Реферальный код не передается
**Признаки:** В логах нет сообщения о реферальном коде

**Решение:**
- Убедитесь, что используете правильный формат URL: `?ref=ABC12345`
- Для Telegram: код должен быть в `start_param` при запуске бота
- Проверьте, что код написан ЗАГЛАВНЫМИ буквами

#### 2. Реферер не найден
**Признаки:** В логах `⚠️ Реферер не найден для кода: ABC12345`

**Решение:**
```bash
# Проверьте, существует ли код в базе
psql -d ton_guarantee
SELECT * FROM users WHERE referral_code = 'ABC12345';

# Если нет - создайте тестового пользователя
curl -X POST http://localhost:3001/api/user/init \
  -H "Content-Type: application/json" \
  -d '{"initData":"dev"}'
```

#### 3. Пользователь уже существует
**Признаки:** В логах `👤 Существующий пользователь`

**Решение:**
- Реферальная связь создается только при первой регистрации
- Для теста используйте инкогнито режим или удалите cookies
- Или удалите пользователя из БД:
  ```sql
  DELETE FROM users WHERE telegram_id = YOUR_TEST_ID;
  ```

#### 4. Регистр букв не совпадает
**Признаки:** Код передается, но реферер не находится

**Решение:**
Код в базе хранится в ВЕРХНЕМ регистре. Backend автоматически конвертирует:
```javascript
referralCode.toUpperCase() // в файле server.js
```

Убедитесь, что эта строка есть в коде.

## Тестирование через API

### 1. Создайте реферера
```bash
curl -X POST http://localhost:3001/api/user/init \
  -H "Content-Type: application/json" \
  -d '{"initData":"dev"}' \
  | jq .user.referralCode
```

Скопируйте полученный код (например, `ABC12345`)

### 2. Создайте реферала
```bash
curl -X POST http://localhost:3001/api/user/init \
  -H "Content-Type: application/json" \
  -d '{"initData":"dev", "referralCode":"ABC12345"}'
```

Проверьте, что в ответе `referredBy` не null

### 3. Проверьте статистику
```bash
# Замените TELEGRAM_ID на ID реферера
curl http://localhost:3001/api/user/TELEGRAM_ID/referrals | jq
```

## Debug Endpoint

Используйте специальный эндпоинт для просмотра всех данных:

```bash
curl http://localhost:3001/api/debug/users | jq
```

Ответ покажет:
- Всех пользователей с их реферальными кодами
- Все реферальные связи
- Количество рефералов у каждого

## Автоматический тест

Запустите тестовый скрипт:

```bash
cd backend
node test-referral.js
```

Он автоматически:
1. Создаст реферера
2. Создаст реферала с правильным кодом
3. Проверит статистику
4. Покажет результаты

## Проверка в Telegram

### 1. Настройте бота
```
@BotFather
/mybots → Ваш бот → Edit Bot → Edit Bot Menu Button
URL: https://your-app.vercel.app
```

### 2. Получите ссылку
В профиле нажмите на контейнер с реферальным кодом.
Ссылка скопируется в формате: `https://t.me/your_bot?start=ABC12345`

### 3. Отправьте другу
Когда друг перейдет по ссылке и запустит бота первый раз,
параметр `ABC12345` будет в `window.Telegram.WebApp.initDataUnsafe.start_param`

### 4. Проверьте логи
```bash
# В консоли backend должно появиться:
📌 Реферальный код из Telegram start_param: ABC12345
✅ Найден реферер
✅ Реферальная связь создана
```

## Частые ошибки SQL

### Ошибка: duplicate key value violates unique constraint
**Причина:** Пытаетесь создать пользователя с существующим telegram_id

**Решение:**
```sql
-- Удалите тестового пользователя
DELETE FROM users WHERE telegram_id = YOUR_ID;
```

### Ошибка: violates foreign key constraint
**Причина:** referred_by указывает на несуществующего пользователя

**Решение:**
```sql
-- Проверьте, что реферер существует
SELECT * FROM users WHERE telegram_id = REFERRER_ID;
```

## Сброс базы данных

Если нужно начать с чистого листа:

```bash
cd backend
psql -d ton_guarantee -f migrations/init.sql
```

⚠️ **ВНИМАНИЕ:** Это удалит все данные!

## Мониторинг в реальном времени

### Логи backend
```bash
tail -f backend.log
```

### Логи PostgreSQL
```bash
# Linux
sudo tail -f /var/log/postgresql/postgresql-15-main.log

# macOS
tail -f /usr/local/var/log/postgresql@15.log
```

### Активные запросы
```sql
SELECT 
  pid,
  now() - pg_stat_activity.query_start AS duration,
  query
FROM pg_stat_activity
WHERE state = 'active'
ORDER BY duration DESC;
```

## Нужна помощь?

1. Проверьте все пункты выше
2. Запустите `node test-referral.js` для автотеста
3. Откройте `http://localhost:3001/api/debug/users` для просмотра данных
4. Проверьте логи backend и браузера

Если проблема сохраняется, создайте Issue на GitHub с:
- Логами из консоли браузера
- Логами backend
- Результатами `curl http://localhost:3001/api/debug/users`