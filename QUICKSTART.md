# 🚀 Быстрый старт - Реферальная система

## Шаг 1: Установка PostgreSQL

### macOS (через Homebrew)
```bash
brew install postgresql@15
brew services start postgresql@15
createdb ton_guarantee
```

### Ubuntu/Debian
```bash
sudo apt update
sudo apt install postgresql postgresql-contrib
sudo systemctl start postgresql
sudo -u postgres createdb ton_guarantee
```

### Windows
1. Скачайте PostgreSQL с [официального сайта](https://www.postgresql.org/download/windows/)
2. Установите и запустите
3. Создайте базу через pgAdmin или командную строку

## Шаг 2: Настройка Backend

```bash
# Клонируйте проект и перейдите в папку backend
cd backend

# Установите зависимости
npm install

# Создайте .env файл
cat > .env << EOF
PORT=3001
DB_USER=postgres
DB_HOST=localhost
DB_NAME=ton_guarantee
DB_PASSWORD=ваш_пароль
DB_PORT=5432
EOF

# Запустите миграции
npm run migrate

# Запустите сервер
npm run dev
```

Сервер должен запуститься на http://localhost:3001

## Шаг 3: Настройка Frontend

```bash
# Вернитесь в корневую папку проекта
cd ..

# Установите зависимости (если не установлены)
npm install

# Создайте .env файл
cat > .env << EOF
REACT_APP_API_URL=http://localhost:3001
REACT_APP_BOT_USERNAME=your_bot_name
EOF

# Запустите React приложение
npm start
```

Приложение откроется в браузере на http://localhost:3000

## Шаг 4: Проверка работоспособности

### Тестирование через скрипт

```bash
# В папке backend запустите тестовый скрипт
cd backend
node test-referral.js
```

Скрипт автоматически:
1. Создаст реферера и получит его код
2. Создаст реферала с этим кодом
3. Проверит статистику
4. Покажет результаты

### Ручное тестирование

1. Откройте http://localhost:3000
2. Вы должны увидеть профиль с тестовыми данными
3. В разделе "Реферальная программа" должен отображаться уникальный код
4. Скопируйте реферальный код (например, `ABC12345`)
5. Откройте в новом инкогнито окне: `http://localhost:3000?ref=ABC12345`
6. Вернитесь в первое окно и обновите страницу
7. В статистике должно появиться: "Приглашено: 1"

### Проверка через консоль браузера

Откройте DevTools (F12) и проверьте логи:
```
📌 Реферальный код из URL параметра: ABC12345
🔄 Инициализация пользователя с реф.кодом: ABC12345
✅ Пользователь инициализирован: {...}
```

### Проверка базы данных

```bash
# Подключитесь к PostgreSQL
psql -d ton_guarantee

# Проверьте пользователей
SELECT telegram_id, first_name, referral_code, referred_by FROM users;

# Проверьте реферальные связи
SELECT r.*, 
       u1.first_name as referrer_name,
       u2.first_name as referred_name
FROM referrals r
JOIN users u1 ON r.referrer_id = u1.telegram_id
JOIN users u2 ON r.referred_id = u2.telegram_id;
```

## 🎉 Готово!

Система реферальных ссылок работает! Теперь вы можете:
- Просматривать свой реферальный код
- Копировать реферальную ссылку
- Видеть статистику рефералов
- Приглашать друзей

## 🐛 Возможные проблемы

### Ошибка подключения к PostgreSQL
```bash
# Проверьте статус PostgreSQL
# macOS
brew services list

# Ubuntu/Debian
sudo systemctl status postgresql

# Проверьте пароль в .env файле
```

### Ошибка "Port 3001 already in use"
```bash
# Найдите процесс на порту 3001
lsof -i :3001

# Убейте процесс
kill -9 <PID>

# Или измените PORT в backend/.env
```

### Таблицы не создаются
```bash
# Проверьте логи миграции
cd backend
npm run migrate

# Если нужно пересоздать таблицы, выполните SQL напрямую
psql -d ton_guarantee -f migrations/init.sql
```

## 📚 Дальше

Теперь можно:
1. Настроить Telegram Bot для реальной интеграции
2. Добавить механизм начисления вознаграждений
3. Развернуть на production сервере

См. полную документацию в [README.md](./README.md)